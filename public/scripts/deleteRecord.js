document.addEventListener("DOMContentLoaded", function(event){
    
    //get the id off all delete button
    let delBtnIds = document.querySelectorAll(".del-button");

    // console.log(delBtnIds);
    
    
    
    
    
    
    console.log("Loaded!");
});